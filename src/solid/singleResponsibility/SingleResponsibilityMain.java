package solid.singleResponsibility;

public class SingleResponsibilityMain
{
    //Single Responsibility:  there should never be more than one reason for a class to change
    //Violation: if we need to change the class because of 3 example reasons:
    // communication protocol changes
    // message format changes
    // the parameters of communication (authentication)


}
